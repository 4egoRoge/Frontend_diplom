const SUB_DOMAIN = 'video';
const URL = 'https://' + SUB_DOMAIN + '.future-mission.ru/api';
const API_URL = URL + '/api';
const PAGE_URL = SUB_DOMAIN + '.future-mission.ru/#';

export {
    API_URL, PAGE_URL
}